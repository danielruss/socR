# Extended SOC 1980 codes

Takes valid 1980 standardized codes (the ones in the book) and extends
them so that unit codes are always the most detailed (even if it is
exactly the same as the parent code.)

## Usage

``` r
extend_standard_soc1980_codes(codes)
```

## Arguments

- codes:

  The codes we are extending

## Value

extended codes.
